{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Run APS",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}